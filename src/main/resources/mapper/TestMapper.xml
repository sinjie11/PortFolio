<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.portFolio.mapper.TestMapper">

    <select id="selectUserList" resultType="com.main.portFolio.vo.TestVO">
        SELECT 
        	ID AS id
        	, NM AS nm
        FROM 
        	TB_USER
    </select>
    
</mapper>